# Optional heavy alignment backend. Install only when ALIGN_BACKEND=whisperx.
# Adds accurate word-level timestamps via forced alignment.
# Installing pulls in torch + faster-whisper and significantly increases image size.
whisperx==3.3.1
soundfile==0.13.0
